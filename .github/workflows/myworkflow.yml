name : Print Hello World

on : 
  push :
jobs : 
  print_macos:
    runs-on: macos-latest

    defaults:
      run:
        working-directory: backend
    steps : 
      - name : Clone repo
        uses : actions/checkout@v4   # repo clone karta hai checkout

      - name : Install dependencies
        run : npm ci 

      - name : Run tests
        run : npm test
